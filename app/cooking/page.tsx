import type { Metadata } from 'next'

export const metadata: Metadata = {
    title: 'foo',
    description: 'bar',
}

export default function CookingPage() {

    return (
        <>

        </>
    )
}